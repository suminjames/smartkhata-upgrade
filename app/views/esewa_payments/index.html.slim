h4 Esewa Payments

table.table.table-striped
  thead
    tr
      th Amount
      th Service Charge
      th Delivery Charge
      th Tax Amount
      th Total Amount
      th Response Reference ID
      th Response Amount
      th Status
      th Request Sent at
      th Response Received at
      th Actions
  tbody
    - @esewa_payments.each.with_index(1) do |esewa_payment, index|
      tr
        td = index
        td = esewa_payment.amount
        td = esewa_payment.service_charge
        td = esewa_payment.delivery_charge
        td = esewa_payment.tax_amount
        td = esewa_payment.total_amount
        td = esewa_payment.response_ref
        td = esewa_payment.response_amount
        td = esewa_payment.status
        td = esewa_payment.request_sent_at
        td = esewa_payment.response_received_at
        td
          = link_to 'Destroy', esewa_payment, :method => :delete, :data => { :confirm => 'Are you sure?' }, class: 'btn btn-danger'

          a.btn.btn-link aria-controls="collapse-#{esewa_payment.id}" aria-expanded="false" data-toggle="collapse" href="#collapse-#{esewa_payment.id}"
            | Payment Verification

      tr
        td colspan="11"
          .collapse id="collapse-#{esewa_payment.id}"
            .row
              .col-sm-4 Request Sent At
              .col-sm-4 Request Received At
              .col-sm-4 Status
            - esewa_payment.esewa_transaction_verifications.each do |verification|
              .row
                .col-sm-4 = verification.request_sent_at
                .col-sm-4 = verification.response_received_at
                .col-sm-4 = verification.status

