<% provide(:section_title, "Upload File") %>
<p> Uploaded file should be in csv format.. Please make sure you know what you are doing</p>
<div>
  <%= form_tag import_files_sys_admin_tasks_path, multipart: true do %>
      <div class="form-group row">

        <div class='col-sm-3'>
          <h5>Select file type</h5>
          <%= select_tag "file_type", options_for_select(@allowed_files_list.map { |c| [c.titleize, c] }) %>
        </div>
      </div>

      <div class="form-group">
        <%= file_field_tag :file %>
      </div>
      <div class="form-group">
        <%= button_tag "Import", { data: { disable_with: "<i class='fa fa-refresh fa-spin'></i> Uploading..." } , :class => 'btn btn-primary btn-flat'}  %>
      </div>

  <% end %>
</div>